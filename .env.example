# ===== DATABASE CONFIGURATION =====
# Untuk lokal: gunakan config MySQL lokal ini
# Untuk Railway: Railway akan auto-set DATABASE_URL saat menambahkan MySQL plugin
# Jadi Anda hanya perlu set di Railway jika customize (usually tidak perlu)

# Option 1: Set DATABASE_URL langsung (MySQL URL format)
# DATABASE_URL=mysql+pymysql://root:password@localhost:3306/chatbot_db

# Option 2: Set config individual (jika tidak set DATABASE_URL, app akan gunakan ini)
DB_USER=root
DB_PASS=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=chatbot_db

# ===== FLASK CONFIG =====
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production

# ===== RAILWAY DEPLOYMENT =====
# Saat deploy di Railway:
# 1. Tambahkan MySQL plugin di Railway (akan auto-set DATABASE_URL)
# 2. Tidak perlu set variable di atas jika Railway DATABASE_URL sudah ada
# 3. Opsional: set SECRET_KEY di Railway environment
